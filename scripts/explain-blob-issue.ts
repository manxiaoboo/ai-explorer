console.log(`
================================================================================
🔍 Vercel Blob 权限机制详解
================================================================================

问题核心：Vercel Blob 存储桶级别的访问控制 vs 文件级别的 access 参数

📋 两个层面的权限：

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1️⃣ 存储桶级别 (Bucket Level) - 在 Vercel 控制台配置                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ • public  - 允许公开访问所有文件（除非单独设为 private）                      │
│ • private - 默认所有文件都需要授权访问（即使代码中设为 public）               │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2️⃣ 文件级别 (File Level) - 代码中通过 access 参数设置                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ • access: 'public'  - 文件URL公开可访问                                      │
│ • access: 'private' - 文件URL需要签名才能访问                                │
└─────────────────────────────────────────────────────────────────────────────┘

🚨 关键问题：

当存储桶被配置为 PRIVATE 时：
  ┌────────────────────────────────────────────────────────────────┐
  │  存储桶: PRIVATE                                                │
  │  ├── 文件A: access='public'  → ❌ 仍然无法访问！               │
  │  └── 文件B: access='private' → ❌ 需要签名                      │
  └────────────────────────────────────────────────────────────────┘

当存储桶被配置为 PUBLIC 时：
  ┌────────────────────────────────────────────────────────────────┐
  │  存储桶: PUBLIC                                                 │
  │  ├── 文件A: access='public'  → ✅ 可以访问                     │
  │  └── 文件B: access='private' → ❌ 需要签名                      │
  └────────────────────────────────────────────────────────────────┘

================================================================================
💡 我们的情况
================================================================================

存储桶配置: PRIVATE (在 Vercel 控制台设置)
代码中设置: access: 'public' (试图让文件公开)

结果: ❌ 文件URL返回 403 Forbidden

原因: 存储桶级别的 PRIVATE 设置覆盖了文件级别的 public 设置

================================================================================
✅ 解决方案对比
================================================================================

方案A: 修改存储桶配置为 PUBLIC
  • 需要登录 Vercel 控制台
  • 在 Project Settings → Blob 中修改
  • 风险: 存储桶内所有文件都可能被公开访问

方案B: 使用签名的临时URL (当前代码已支持)
  • 使用 \`get\` 函数生成带签名的URL
  • 例如: \`get('filename', { token: process.env.BLOB_READ_WRITE_TOKEN })\`
  • 缺点: URL有过期时间，不适合长期存储在数据库

方案C: 直接使用外部Logo URL (我们选择的方案)
  • 使用 Clearbit / Google / DuckDuckGo 等免费服务
  • 无需管理存储和权限
  • 可靠且维护成本低

================================================================================
`);
